= tabs_tag do |tab|
  = tab.expense 'Expense', expenses_path
  = link_to_unless_current('List', expenses_path)
  = link_to_unless_current('Add', new_expense_path)
  = link_to_unless_current('Edit', edit_expense_path(@expense)) if (@expense && !@expense.new_record?)
  = link_to_unless_current('Detail', expense_path(@expense)) if (@expense && !@expense.new_record?)
  
  = tab.expense_type 'Expense Type', exp_types_path
  = link_to_unless_current('List', exp_types_path)
  = link_to_unless_current('Add', new_exp_type_path)
  = link_to_unless_current('Edit', edit_exp_type_path(@exp_type)) if (@exp_type && !@exp_type.new_record?)
  = link_to_unless_current('Detail', exp_type_path(@exp_type)) if (@exp_type && !@exp_type.new_record?)

  = tab.payment_type 'Payment Type', payment_types_path
  = link_to_unless_current('List', payment_types_path)
  = link_to_unless_current('Add', new_payment_type_path)
  = link_to_unless_current('Edit', edit_payment_type_path(@payment_type)) if (@payment_type && !@payment_type.new_record?)
  = link_to_unless_current('Detail', payment_type_path(@payment_type)) if (@payment_type && !@payment_type.new_record?)